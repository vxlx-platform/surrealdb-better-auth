{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.json",
  "include": ["tests/**/*.ts", "vitest.config.ts", "vitest.live.config.ts"],
  "exclude": ["dist", "node_modules"]
}
